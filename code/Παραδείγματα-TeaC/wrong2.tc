

const N <- 100;									-- missing type

let a, b: int;

const cube <- (i: int): int => {
  return i*i*i > 3;								-- unrecognized token
};    
   
const add <- (n: int, k: int): int => {
  let j: int;
  
  j <- (N-n + cube(k);							-- unclosed parenthesis
  writeInt(j);
  return j;
};

(* Here you can see some useless lines.
 * Just for testing the multi-line comments ...
 *)
const start <- (): int => { 
  a <- readInt();
  b <- readInt();
  add(a, b);
 	         
 return 0;
}
